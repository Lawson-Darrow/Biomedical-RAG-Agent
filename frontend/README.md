# Frontend (Milestone 6)

React + Vite demo for the agent, matching the stack used on ledarrow.dev.
Scaffolded in Milestone 6 once the API contract (`api/main.py`) is stable.

Planned: a question box → grounded answer with inline, clickable citations and a
visible "insufficient evidence" abstention state.
